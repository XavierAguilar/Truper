const fs = require('fs'); const { createCanvas, loadImage } = require('canvas'); loadImage('images/tigre_esquina_inferiror_derecha.jpg').then(img => { const canvas = createCanvas(img.width, img.height); const ctx = canvas.getContext('2d'); ctx.drawImage(img, 0, 0); const imgData = ctx.getImageData(0, 0, canvas.width, canvas.height); const data = imgData.data; for (let i = 0; i < data.length; i += 4) { const r = data[i], g = data[i+1], b = data[i+2]; if (r > 240 && g > 240 && b > 240) { data[i+3] = 0; } } ctx.putImageData(imgData, 0, 0); const out = fs.createWriteStream('images/tigre_esquina_inferiror_derecha.png'); const stream = canvas.createPNGStream(); stream.pipe(out); out.on('finish', () => console.log('PNG creado!')); });
